{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-window",
  "description": "The local DataForge Workbench window may call only typed DataForge backend commands.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
